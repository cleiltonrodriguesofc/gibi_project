services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app          # live reload for development
      - db_data:/app/data_db
    env_file:
      - .env
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  db_data:
